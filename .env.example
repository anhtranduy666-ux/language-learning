# Supabase — dùng cho audio phát âm (xem docs/audio-tts.md).
# Thiếu hai biến này thì phần audio từ xa tự tắt và app lùi về giọng đọc của
# hệ điều hành, nên `npm run dev` vẫn chạy ngay khi mới clone repo.
VITE_SUPABASE_URL=https://<ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon key>
